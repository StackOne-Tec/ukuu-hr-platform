# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# Firebase service account private key — never commit
Ukuu IAM Admin chat.json

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/

# local app artifacts (not part of the source)
/db/
/download/
/scripts/
/upload/
/worklog.md
/agent-ctx/
--full-page
*.pid
# admin-mode dev build dir (runs alongside .next)
/.next-admin/

# playwright test output
/test-results/
/playwright-report/

# Firebase service account key — contains a private key, never commit.
UKUU Firebase Admin SDK.json
*.service-account.json
*serviceAccount*.json

# Local fallback data store (used when no Firebase service account is configured)
.data/
